version: 2.1
docker:
  - image: circleci/node:latest-browsers
jobs:
  build-and-deploy:
    steps:
      - run: yarn install
      - run: yarn deploy
workflows:
  build-and-test:
    jobs:
      - build-and-deploy
